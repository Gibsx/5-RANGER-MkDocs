# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Runtime state — never commit. These show up if you run sync.py or
# entry.py locally to smoke-test outside a Pterodactyl container.
state.json
mkdocs.yml
/docs/
/site/

# Pterodactyl egg manifest — kept locally at repo root for easy import
# into the panel, but not version-controlled. Eggs are environment-
# specific (docker image tag, container UID) and shouldn't diverge
# across clones; regenerate locally as needed from a known-good source.
/egg-ranger-aide-memoire.json

# Editor noise
.vscode/
.idea/
*.swp
